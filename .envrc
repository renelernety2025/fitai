# direnv: pin AWS account for this project so bare `aws`/`terraform`
# never hit the default profile (which points at a different account).
# Run `direnv allow` once after creating/changing this file.
export AWS_PROFILE=fitai
