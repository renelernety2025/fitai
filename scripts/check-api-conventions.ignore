# scripts/check-api-conventions.sh — intentional exceptions
# Format: paste the offending grep match line exactly. One per line.

# (No exceptions currently. SNS webhook handler uses @Body() body: unknown,
# which passes Check 1 natively since `unknown` is not in the raw-type regex.)
