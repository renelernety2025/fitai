// Re-export everything from the split api/ directory.
// This file exists for backwards compatibility — all imports
// like `import { getExercises } from '@/lib/api'` still work.
export * from './api/index';
